// shortcut: CTRL + SHIFT + D
/**DOC**
Duplicates the current page at the end of the page list.
**DOC**/
try {
  fw.runScript(fw.appJsCommandsDir + "/bs.js");
} catch(e){
  alert("This command requires the bs.js library\rGet it at http://github.com/bomberstudios/fireworks/");
};

original_name = dom().pageName;
dom().duplicatePage(dom().currentPageNum);
dom().changeCurrentPage(Pages.count() - 1);
dom().setPageName(dom().currentPageNum, original_name + ' (copy)');