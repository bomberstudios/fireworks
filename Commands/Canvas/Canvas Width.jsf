// shortcut: CTRL + ALT + W
/**DOC**
Sets the width of the canvas.
**DOC**/
try {
  fw.runScript(fw.appJsCommandsDir + "/bs.js");
} catch(e){
  alert("This command requires the bs.js library\rGet it at http://bomberstudios.com/fireworks/");
};

var default_width = dom().width;

var w = prompt("Canvas width in pixels",default_width);

dom().setDocumentCanvasSize({left:0, top:0, right:eval(w), bottom: dom().height}, true);
